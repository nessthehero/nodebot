nodebot
=======

IRC bot in Node.js

Nodebot needs a config file.

It looks like this:

Store in ./config/index.js

    exports.config = {
        "server": "morgan.freenode.net",
        "nick": "NodeBot",
        "channel": "#nodejs"
    }

